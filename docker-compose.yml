version: '3.9'

services:
    bot-service:
        container_name: bot-service
        image: razom-pay/bot-service:dev
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        env_file:
            - .env.production.local
        networks:
            - razom-pay

networks:
    razom-pay:
        external: true
